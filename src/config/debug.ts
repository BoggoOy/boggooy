/**
 * Debug configuration for the application.
 *
 * Toggle these flags to enable/disable debug tools.
 * Set all to `false` before deploying to production.
 */
export const debug = {
  /** Show Leva UI controls for GrainyNoiseBackground color tuning */
  leva: false,
} as const;
